import SwiftUI

struct ContentView: View {
    var body: some View {
        Button("OK") {
            // TODO: handle tap
        }
        .buttonStyle(.borderedProminent)
        .controlSize(.large)
    }
}

#Preview {
    ContentView()
}
